The focal method is `${focal_method}` in focal class `${class_name}`, the information is ```${information}```.
<#if other_methods?has_content>
    Signatures of Other methods in the focal class are `${other_methods}`.
</#if>
